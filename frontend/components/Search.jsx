import { COLORS } from '../variables';

const Search = () => (
  <div className='Search'> Search

    <style jsx>{`
      .Search {
        flex: 1;
        background: ${COLORS.blue};
        color: ${COLORS.white};
      }
    `}</style>
  </div>
);

export default Search;