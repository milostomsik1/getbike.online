import styled from 'styled-components';
import { COLORS } from '../../variables';

const PremiumAds = styled.div`
  background: ${COLORS.charcoal};
  color: ${COLORS.white};
`;

export default () => (
  <PremiumAds></PremiumAds>
);